# 导师版式红线（必须遵守）

以下规则为当前论文排版的硬约束，后续改稿一律按此执行：

1. 双栏页面左右栏底部必须尽量齐平，避免明显“参差不齐”。
2. 伪代码中的单行不允许因为超出少量字符而断行；应通过精简或改写，使其保持单行。
3. 避免出现类似“value of”这类尴尬断行（行末只剩1-2个词后接公式/换行）。
4. 伪代码风格统一为数据库论文经典风格（命名、缩进、语句形态一致）。
5. 避免第5.2节那种额外缩进的大段块状说明（如 point-wise pruning step 的块状视觉），保持正文风格统一。
6. 公式不得越界（不允许超出栏宽/页边界）。
7. 层级结构必须清晰稳定（标题层级、并列关系、视觉强弱一致）。

---

执行方式（每轮改稿后自检）：
- 检查双栏齐平与异常换行；
- 检查伪代码是否有断行和风格漂移；
- 检查公式越界；
- 检查章节层级一致性。
