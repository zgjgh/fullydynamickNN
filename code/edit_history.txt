2023/11/22：

1. In main document, change the threshold from 12 to 1000;
2. In the HDR_Tree document:
   In the function of construct_i: add a function on the 854 line that fanout=3000;
                                   in the line 866, change the parameter T from data_i->T[0] to data_i->T[5];
    
   In the function of userComputeKNN: shadow the function project_u_iclust_pca_init(user);
                                      and add:
                                      u_trans_i_clus_init.clear();
                                      and add:
                                      u_trans_i_clus_init.emplace_back((data_i->T[5])*(user->center));

   In the function of construction:  shadow:
                                    for(auto c_item_1:*(dictionary_for_layer_itemcluster[Cj->l-1])){
                                       (Cj->distance_cross_u_t).emplace_back((Cj->center-c_item_1->center).norm());
                                    }
                                     shadow:
                                    for(auto c_item_0:*(dictionary_for_layer_itemcluster[Cj->l-1])){
                                       (Cj->distance_cross_u_t).emplace_back((Cj->center-c_item_0->center).norm());
		                    }


2023/11/27

1. In the main document, shadow the constructing of the sphere tree and shadow the constructing of the i-distance index structure
   In the main document, shadow the constructing of the clusters for batch updating
2. In the HDR_Tree document, shadow the sentence for calculating the distance from the clusters on the HDR Tree and the clusters to the Delta Tree
   In the HDR_Tree document, shadow the sentence for calculating the low dimensional version of the users and the items


2023/12/1
1. In the HDR_Tree document, in the function of construct, change the dimensions of each level to 8 by setting all the level as 6(data->T[5]).
2. In the HDR_Tree document, in the function of projectitem, in the line that iTrans.emplace_back(data->T[i] * (I->v)), change i to 5
